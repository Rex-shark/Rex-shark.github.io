flowchart TB
subgraph Sources[新聞來源]
LTN[網路新聞]
TC[TechCrunch]
end

    subgraph Trigger[觸發方式]
        Schedule[⏰ 排程任務<br/>Spring Scheduled]
        Manual[🖱️ 手動 API<br/>curl POST]
    end

    subgraph App[ThreadsBot 應用程式]
        Crawler[🕷️ 爬蟲服務<br/>WebMagic]
        AiGen[🤖 AI 生成服務<br/>Spring AI]
        Publisher[📤 發文服務<br/>Threads API]
    end

    Ollama[("🧠 本地 LLM<br/>Ollama + gemma4-e4b")]

    subgraph DB[PostgreSQL]
        T1[crawl_article<br/>爬蟲文章]
        T2[ai_generated_post<br/>AI 生成結果]
        T3[post_publish_log<br/>發文紀錄]
    end

    Threads[🧵 Threads 平台]

    Sources -->|HTTP| Crawler
    Trigger --> Crawler
    Trigger --> AiGen
    Trigger --> Publisher

    Crawler -->|存入| T1
    T1 -->|讀取 PENDING| AiGen
    AiGen <-->|改寫請求| Ollama
    AiGen -->|存入| T2
    T2 -->|讀取 DRAFT| Publisher
    Publisher -->|POST /threads<br/>POST /threads_publish| Threads
    Publisher -->|記錄結果| T3

    style Ollama fill:#e1f5ff,stroke:#0288d1
    style Threads fill:#f3e5f5,stroke:#7b1fa2